import { CounterCharactersPipePipe } from './counter-characters-pipe.pipe';

describe('CounterCharactersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CounterCharactersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
